// import { RECEIVE_MYCURRENCY } from "../actions/customize_actions";

// export const nullInfo = () => ({ username: "", currency: 1000 });

// const customizeReducer = (state = nullInfo(), action) => {
//   switch (action.type) {
//     case RECEIVE_MYCURRENCY:
//       return Object.assign({}, state, action.payload);
//     default:
//       return state;
//   }
// };

// export default customizeReducer;
